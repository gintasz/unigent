## API Report File for "@microfoom/opencode-adapter"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { OpenSession } from '@microfoom/core';
import { StreamEvent } from '@microfoom/core';
import type { UsageDelta } from '@microfoom/core';

// @public
export function createOpenCodeOpenSession(options?: OpenCodeSessionOptions): OpenSession;

// @public (undocumented)
export const OPENCODE_VERSION = "0.1.0";

// @public
export interface OpenCodeBackend {
    close: () => Promise<void>;
    createSession: () => Promise<string>;
    forkSession: (parentId: string) => Promise<string>;
    // Warning: (ae-forgotten-export) The symbol "TurnOutcome" needs to be exported by the entry point index.d.ts
    prompt: (sessionId: string, spec: PromptSpec) => Promise<TurnOutcome>;
}

// @public
export type OpenCodeBackendFactory = (args: {
    readonly config: OpenCodeConfig;
}) => Promise<OpenCodeBackend>;

// @public
export type OpenCodeConfig = Record<string, unknown>;

// @public
export interface OpenCodeSessionOptions {
    readonly backendFactory?: OpenCodeBackendFactory;
    readonly serverName?: string;
}

// @public
export interface PromptSpec {
    readonly model: {
        readonly providerID: string;
        readonly modelID: string;
    };
    // (undocumented)
    readonly onEvent?: ((event: StreamEvent) => void) | undefined;
    readonly prompt: string;
    readonly serverName: string;
    // (undocumented)
    readonly signal?: AbortSignal | undefined;
    readonly system: string;
    readonly tools?: Record<string, boolean> | undefined;
}

```
