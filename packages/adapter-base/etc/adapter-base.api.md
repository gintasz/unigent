## API Report File for "@microfoom/adapter-base"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import type { NeutralToolDef } from '@microfoom/core';
import { SessionTurnResult } from '@microfoom/core';
import { UsageDelta } from '@microfoom/core';

// @public
export const asArray: (value: unknown) => readonly unknown[];

// @public
export const asNumber: (value: unknown) => number;

// @public
export const asObject: (value: unknown) => Json | undefined;

// @public
export const asString: (value: unknown) => string | undefined;

// @public
export interface CliProcess {
    // (undocumented)
    kill: () => void;
    // (undocumented)
    readonly lines: AsyncIterable<string>;
    stderr: () => string;
}

// @public
export function createMcpHandler(tools: readonly NeutralToolDef[], serverName: string, describe?: DescribeTool): {
    handle: (request: JsonRpcRequest) => Promise<Record<string, unknown> | null>;
    terminated: () => boolean;
};

// @public
export type DescribeTool = (tool: NeutralToolDef) => string;

// @public
export function drainTurnStream(proc: TurnProcess, handle: (event: Json) => void): Promise<void>;

// @public
export const EMPTY_USAGE: UsageDelta;

// @public
export type Json = Record<string, unknown>;

// @public
export interface McpServerHandle {
    close: () => Promise<void>;
    terminated: () => boolean;
    readonly url: string;
}

// @public
export function resolveTurnResult(reader: TurnReaderState, proc: TurnProcess, label: string): SessionTurnResult;

// @public
export interface SpawnLineOptions {
    readonly cwd?: string;
    readonly env?: NodeJS.ProcessEnv;
    readonly onExit?: () => void;
    readonly prepare?: () => void;
    // (undocumented)
    readonly signal?: AbortSignal;
}

// @public
export function spawnLineProcess(command: string, args: readonly string[], options?: SpawnLineOptions): CliProcess;

// @public
export function startMcpServer(tools: readonly NeutralToolDef[], serverName: string, describe?: DescribeTool): Promise<McpServerHandle>;

// @public
export function toolDescription(tool: NeutralToolDef): string;

// @public
export interface TurnError {
    // (undocumented)
    readonly message: string;
    readonly retryable: boolean;
}

// @public
export interface TurnProcess {
    // (undocumented)
    readonly lines: AsyncIterable<string>;
    stderr: () => string;
}

// @public
export interface TurnReader extends TurnReaderState {
    handle: (event: Json) => void;
    sessionId: () => string | undefined;
}

// @public
export interface TurnReaderState {
    assistantText: () => string;
    error: () => TurnError | undefined;
    resultSeen: () => boolean;
    usage: () => UsageDelta;
}

// Warnings were encountered during analysis:
//
// src/mcp.ts:67:3 - (ae-forgotten-export) The symbol "JsonRpcRequest" needs to be exported by the entry point index.d.ts

```
